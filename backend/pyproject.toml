[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = ["migrations/versions", "migrations/env.py", "migrations/script.py.mako"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.12"
exclude = ["migrations"]
