# --- Phase 0: core web + db stack ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9
neo4j==5.24.0
redis==5.0.8
celery==5.4.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
structlog==24.4.0
prometheus-fastapi-instrumentator==7.0.0
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# --- Later phases (pinned here per DATA_CONTRACT.md, installed when those phases start) ---
# torch, torch-geometric        # Phase 4 — GNN
# spacy, sentence-transformers  # Phase 2/3 — NLP
# rasterio, opencv-python, sentinelhub  # Phase 3 — vision
# anthropic                     # Phase 7 — briefings
# networkx                      # Phase 1 — synthetic generator
