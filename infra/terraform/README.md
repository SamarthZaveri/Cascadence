# infra/terraform

Stretch goal (PRD.md §10, Phase 8). Not implemented yet — the deployment target for now
is Docker Compose on a single VM with a Caddy/nginx reverse proxy (see README.md
"Deployment"). This directory exists as a placeholder so the eventual IaC has an obvious
home; fill in when Phase 8 starts.
